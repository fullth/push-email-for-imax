name: Check CGV seats

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - uses: actions/cache@v4
        with:
          path: /tmp/state.sqlite3
          key: seat-state-${{ github.repository }}-${{ github.run_id }}
          restore-keys: |
            seat-state-${{ github.repository }}-
      - run: python -m app.check_once
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          CGV_ENDPOINT: unused
          CGV_REQUEST_JSON: "{}"
          CGV_HEADERS_JSON: "{}"
          CGV_COOKIES_JSON: "{}"
          CGV_STATE_PATH: /tmp/state.sqlite3
          SMTP_HOST: ${{ vars.SMTP_HOST }}
          SMTP_PORT: ${{ vars.SMTP_PORT }}
          SMTP_USERNAME: ${{ vars.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_USE_TLS: ${{ vars.SMTP_USE_TLS }}
          MAIL_FROM: ${{ vars.MAIL_FROM }}
          MAIL_TO: ${{ vars.MAIL_TO }}
